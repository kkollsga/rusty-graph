# Skip specific directories
/pytest/
/venv/
/.venv/
/.venv*/
/.claude/
/.pytest_cache/
/data/
/dev-documentation/
/bench/
/inbox/

# Ignore Rust build artifacts (keep wheels for distribution)
/target/
!/target/wheels/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Compiled extension modules (built by maturin develop)
*.so
*.pyd

# IDE and editor settings
.vscode/

# Performance baselines (dev artifacts)
/baselines/
/.benchmarks/

# Hypothesis database (property-based testing cache)
/.hypothesis/

# Coverage data
.coverage
coverage.xml

# Generated graph files
*.kgl
# …except the small Cat G-N regression fixtures (5.9 KB total, byte-deterministic
# from tests/fixtures/build_fixtures.py with a seeded RNG). These are tracked
# in-repo so CI sees them; reproducible via the script in the inbox cover
# message (inbox/read/2026-05-13-from-mcp-servers-cat-G-N-fixtures.md).
!tests/fixtures/*.kgl

# Sphinx docs build output
docs/_build/
docs/autoapi/

# macOS system files
.DS_Store
/projects/
